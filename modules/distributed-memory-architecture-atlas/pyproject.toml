[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "distributed-memory-architecture-atlas"
version = "2.1.0"
description = "A unified laboratory for combinatorial, spectral, probabilistic, sheaf-theoretic and information-theoretic distributed-memory architectures."
requires-python = ">=3.10"
dependencies = ["cryptography>=46.0.0"]

[tool.setuptools.packages.find]
where=["src"]
