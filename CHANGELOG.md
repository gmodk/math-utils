# Changelog

## 1.2.0 — 2026-09-15

- Replace the fourth module with the supplied ML Knowledge Graph fork at commit `c7e20bf18490f3f1ba0afd8f3181f595e0407ca7`, retaining its interface, bundled graph, attribution and video API.
- Add reviewed CSV import, canonical node/edge template downloads, actionable errors, non-destructive failure and browser-local snapshots.
- Preserve separate relation/direction/measurement/provenance fields, Notion IDs, URLs, parallel evidence and self-loops through APIs, exploration and JSON export.
- Integrate the supplied 2D/3D constellation, semantic layouts, directed traversal, adaptive labels, gravity, graph filtration and independent quadrilateral area filling. Python computes graph math and TDA through local JSON endpoints.
- Vendor the fork's browser dependencies for offline operation; add `/api/health` and reuse launcher port offset 4. No new production Python dependency.
- Preserve all 173 files in Atlas, Markov and Sₙ from the start-of-integration state, including existing Sₙ changes. Keep a verified recoverable backup of the removed module outside the repository.
- Add original-source golden parity, API/template/validation tests, browser workflow checks, provenance documentation and a verified ZIP/extraction launcher.
- Document seeded force-layout coordinates and exact pairwise forces in the fork's Python port; this changes coordinates relative to its original randomized D3 approximation. Ship and health-check a precomputed cache for the bundled graph.
- Final preservation review tightened URL/provenance alias isolation, retained zero-valued force weights, removed runtime-only fields from canonical export, kept missing relations null, and suppressed arrows for undirected edges in both base and highlighted rendering.

The entries below describe historical releases, not the 1.2 shipped module inventory.

## 1.0.0 — 2026-09-14

### Added

- Unified Math Utils launcher and responsive landing page.
- Shared first-run Python environment for all server-backed modules.
- Independent process and port isolation for the four original applications.
- Live landing-page readiness indicators.
- One-command Windows, macOS, and Linux startup.
- Integration tests and validation documentation.

### Integrated unchanged

- Distributed Memory Architecture Atlas 2.1.0 with all five distributed-memory approaches and laboratories.
- Markov Chain Explorer Python Edition 2.0.0 with its prebuilt JavaScript UI and interactive 3D hypergraph.
- Sₙ Explorer WebUI with its Python group-theory engine.
- Correlation & Regression Explorers bundle with all eleven standalone explorers.

No module interface, mathematical method, API endpoint, route, or internal source file was rewritten by the integration layer.

## 1.1.0 — 2026-09-14

- Rebuilt and validated the outer integration layer; all 180 vendored module files remain byte-identical.
- Always select the repository's shared `.venv`; check installed distribution versions before installing requirements.
- Correct the development manifest to include runtime dependencies and HTTPX.
- Reject invalid port ranges and unsupported IPv6 binds; check the actual bind address for occupied ports.
- Clean up children after partial startup failures, wait after forced shutdown, and stop the platform if a child exits.
- Report each module ready at startup, with a bounded startup timeout.
- Keep landing readiness polling active after startup and preserve keyboard focus during updates; improve narrow card wrapping.
- Add Windows Python-launcher fallback and UTF-8 startup mode.
- Add live integration checks, source/inline-JavaScript validation, retained-file checksums, and a verified clean ZIP builder.
